from setuptools import setup, find_packages

setup(
    name="hifan--msutils",
    version="0.0.0",
    packages=["msutils"],
    # entry_points={
    #   'console_scripts': [
    #       'libgencli = libgencli.__main__:main'
    #   ]
    # },
    )