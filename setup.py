from setuptools import setup, find_packages

setup(
    name="hifan--mseutils",
    version="0.0.0",
    packages=["mseutils"],
    # entry_points={
    #   'console_scripts': [
    #       'libgencli = libgencli.__main__:main'
    #   ]
    # },
    )